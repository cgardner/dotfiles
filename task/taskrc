# vim: foldmethod=marker
# Use the command 'task show' to see all defaults and overrides

# Config {{{
data.location=~/.task
calendar.holidays=full
recurrence.confirmation = no

# Hide the revision information {{{
journal.info = off
journal.time = on
# }}}
# }}}

# Includes {{{
include ~/.dotfiles/task/attributes.conf
include ~/.dotfiles/task/inbox.conf
include ~/.dotfiles/task/reports.conf
include ~/.dotfiles/task/urgency.conf
include ~/.dotfiles/task/context.conf
include ~/.dotfiles/task/sync.conf
include ~/.dotfiles/task/solarized.theme
# }}}
